// Mirrors `core/config.py::STARTING_BALANCE_CENTS` -- display only, the
// server grants the real amount at signup consume time regardless of
// what this says.
export const STARTING_BALANCE_CENTS = 1_000_000;

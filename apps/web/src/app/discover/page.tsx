export default function DiscoverPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold">Discover</h1>
      <p className="text-muted-foreground mt-2">
        Artist list + discovery feeds (derived client-side from the same list, per
        ARCHITECTURE.md). Real data plumbing lands in build step 3.
      </p>
    </div>
  );
}
